<% if @thermostats.any? %>
  <div class="thermostats ui items">
    <%= render partial: 'thermostat', collection: @thermostats %>
  </dir>
<% else %>
  <%= link_to 'Add thermostat', new_thermostat_path  %> -->
<% end %>
